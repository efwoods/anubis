"""Client-side integration with the Neural Nexus adapter service (anubis-adapter).

INERT SALVAGE — nothing imports this package yet. Salvaged from the
``z-anubis`` branch (commit ``d1b8473``) on 2026-09-09. ``client.py`` is a
verbatim copy and is complete and importable, but no live code path calls it,
so its presence changes no behaviour. See ``src/api/salvage/README.md`` for
the activation checklist.
"""
