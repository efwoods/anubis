"""Tracked source data for the Anubis pipeline.

Most of ``data/`` is runtime/generated output (adapter datasets, downloaded
corpora) and is gitignored. A small number of source modules — such as the
standardized identity-question bank — live here and are tracked via explicit
negations in ``.gitignore``.
"""
