# Plan
Testing:
update or delete memory via text
improve RAG retrieval (HNSW) and remove duplicate retrieval

create new memory from text

preprocessing creates the dataset (improvements)
update the scanned features (quick update)

train adapter with the new datasets using Fireworks.ai and GRPO 
