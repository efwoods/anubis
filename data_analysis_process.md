# Ingest data 
# analyze syntax and structure
# analyze traces and choices throughout a conversation

# Compare and contrast the prompt injected features with respect to those features before and after analyzing for those features and prompt injecting

# Example
- analyze text
- prompt inject
- create standardized questions with respect to those features 
- query the model for the responses to those questions with both system prompts
- Analyze the responses with respect to the same original features in order to identify if there is a measureable change of OCEAN characteristics before and after prompt injection. There needs to be a non-trivial sample size.

