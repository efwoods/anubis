apiVersion: 1

datasources:
  - name: MyPostgresData
    type: postgres
    url: host.docker.internal:5432
    user: ${PG_USER}
    database: ${PG_DB}
    secureJsonData:
      password: ${PG_PASSWORD}
    jsonData:
      sslmode: 'disable' # Standard for local dev
      postgresVersion: 15